/** Host loader entry for the MCP settings tab browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the MCP settings tab. */
export function apply(): void {}
